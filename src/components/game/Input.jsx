import LetterButton from "./LetterButton";

const Input = () => {
    return (
        <div>
            <LetterButton/>
            <LetterButton/>
            <LetterButton/>
            <LetterButton/>
        </div>
    );
};

export default Input;
